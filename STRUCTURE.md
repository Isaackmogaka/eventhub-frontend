# Frontend Structure

- app/ — Next.js App Router pages
- lib/api.ts — all backend API calls
- lib/auth.ts — session/user cache helpers
- lib/hooks.ts — useRequireAuth and other shared hooks
- lib/components/ — shared UI components (Sidebar, Skeleton, PasswordInput, etc.)
- lib/toast/ — toast notification system
